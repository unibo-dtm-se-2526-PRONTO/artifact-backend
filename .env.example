#template to create the local, not versioned .env file with the credentials for the db connection

#db
DB_NAME=
DB_USER=
DB_PASSWORD=
DB_HOST=
DB_PORT=

#django
SECRET_KEY=
DEBUG=True
# comma-separated; required once DEBUG is off
ALLOWED_HOSTS=localhost,127.0.0.1
